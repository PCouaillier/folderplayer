{
  "name": "chanplayer",
  "productName": "chanplayer",
  "version": "1.0.0",
  "description": "My Electron application description",
  "main": "dist/app-back/main.js",
  "scripts": {
    "build-dev": "npx tsc -p . && node build.js && npx webpack --mode development",
    "build": "npx tsc -p . && node build.js && npx webpack",
    "start": "npm run build-dev && npx electron ."
  },
  "keywords": [],
  "author": {
    "name": "PCouaillier",
    "email": "paul.couaillier@laposte.net"
  },
  "license": "MIT",
  "dependencies": {
    "electron": "^15.3.0"
  },
  "engines": {
    "node": ">12.0"
  },
  "devDependencies": {
    "@types/node": "12",
    "typescript": "^4.0",
    "webpack": "^5.61.0",
    "webpack-cli": "^4.9.1"
  }
}
